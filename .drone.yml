kind: pipeline
name: default

steps:
- name: edgar
  image: golang:1.11
  commands:
  - export DIR=$GOPATH/src/github.com/tmlbl
  - mkdir -p $DIR
  - cp -r . $DIR
  - ls $DIR
  - cd $DIR/edgar
  - go get ./...
  - go test -v
